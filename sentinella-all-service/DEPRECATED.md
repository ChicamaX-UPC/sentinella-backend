# sentinella-all-service (legacy)

Este módulo queda congelado como referencia histórica durante el corte `big bang`.

- El build principal de `sentinella-platform` ya no lo incluye en `<modules>`.
- El runtime objetivo ahora es `api-gateway + microservicios por bounded context`.
- No agregar nuevas features aquí; todo cambio funcional debe ir a los nuevos servicios.
